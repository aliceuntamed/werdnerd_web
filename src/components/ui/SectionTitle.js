import React from "react";
export function SectionTitle({ children }) {
    return (<h2 className="font-heading text-4xl mb-6 bg-chrome-horizontal bg-clip-text text-transparent tracking-tight">
      {children}
    </h2>);
}
